// enums.js
const Status = ['withdrawed', 'normal', 'suspended'];
const ExpenseStatus = ['complete', 'pending', 'canceled'];
const Menu = ['breakfast', 'lunch', 'snack'];

module.exports = { Status, ExpenseStatus, Menu };
